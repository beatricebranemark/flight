export default function getFilter() {
  let filter = {
    barChart: {
      filter: false,
    },
    map: {
      filter: false,
    },
    personList: {
      filter: false,
    },
  }
  return filter
}
